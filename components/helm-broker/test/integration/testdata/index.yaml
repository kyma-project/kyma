apiVersion: v1
entries:
  redis:
    - name: redis
      description: Redis service
      version: 0.0.1
  acc-testing:
      - name: acc-testing
        description: "Acceptance testing bundle"
        version: 0.0.1

