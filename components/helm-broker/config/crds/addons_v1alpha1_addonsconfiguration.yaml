apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: addonsconfigurations.addons.kyma-project.io
spec:
  group: addons.kyma-project.io
  names:
    categories:
    - all
    - addons
    kind: AddonsConfiguration
    plural: addonsconfigurations
  scope: Namespaced
  subresources:
    # status enables the status subresource.
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            repositories:
              items:
                properties:
                  url:
                    type: string
                required:
                - url
                type: object
              type: array
            reprocessRequest:
              description: ReprocessRequest is strictly increasing, non-negative integer
                counter that can be incremented by a user to manually trigger the
                reprocessing action of given CR.
              format: int64
              type: integer
          required:
          - repositories
          type: object
        status:
          properties:
            lastProcessedTime:
              format: date-time
              type: string
            observedGeneration:
              format: int64
              type: integer
            phase:
              type: string
            repositories:
              items:
                properties:
                  addons:
                    items:
                      properties:
                        message:
                          type: string
                        name:
                          type: string
                        reason:
                          type: string
                        status:
                          enum:
                          - Ready
                          - Failed
                          type: string
                        version:
                          type: string
                      required:
                      - name
                      - version
                      type: object
                    type: array
                  message:
                    type: string
                  reason:
                    type: string
                  status:
                    type: string
                  url:
                    type: string
                required:
                - url
                - addons
                type: object
              type: array
          required:
          - phase
          type: object
  version: v1alpha1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
