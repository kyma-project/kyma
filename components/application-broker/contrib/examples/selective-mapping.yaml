apiVersion: applicationconnector.kyma-project.io/v1alpha1
kind: ApplicationMapping
metadata:
  name: ec-prod
  namespace: stage
spec:
  services:
    # Instance of the ApplicationMapping enables
    # Application service with the given ID in the given namespace.
   - id: ac031e8c-9aa4-4cb7-8999-0d358726ffaa