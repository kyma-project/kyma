apiVersion: serverless.kyma-project.io/v1alpha1
kind: Function
metadata:
  name: demo
spec:
  maxReplicas: 1
  source: |
    def main(event, context):
        return "hello world3"
  deps: |
    djtest==0.1.7
