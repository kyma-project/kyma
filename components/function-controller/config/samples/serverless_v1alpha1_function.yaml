apiVersion: serverless.kyma-project.io/v1alpha1
kind: Function
metadata:
  name: demo
  labels:
    foo: bar
spec:
  source: |
    module.exports = {
        main: function(event, context) {
          return 'Hello World'
        }
      }
