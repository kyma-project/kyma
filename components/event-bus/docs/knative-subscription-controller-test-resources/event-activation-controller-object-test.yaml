apiVersion: applicationconnector.kyma-project.io/v1alpha1
kind: EventActivation
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: eventactivation-sample
spec:
  # Add fields here
  foo: bar
  sourceId: applicationX
  displayName: applicationX-activation