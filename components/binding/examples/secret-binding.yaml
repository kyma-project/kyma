apiVersion: bindings.kyma-project.io/v1alpha1
kind: Binding
metadata:
  name: secret-binding
spec:
  source:
    kind: secret
    name: secret-with-credentials
  target:
    kind: deployment
    name: env-sample
