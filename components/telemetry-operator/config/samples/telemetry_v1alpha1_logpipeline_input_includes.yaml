apiVersion: telemetry.kyma-project.io/v1alpha1
kind: LogPipeline
metadata:
  name: logpipeline-include-sample
spec:
  input:
    application:
      namespaces:
        include:
          - kyma-system
  output:
    custom: |
      Name               stdout
