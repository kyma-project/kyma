apiVersion: applicationconnector.kyma-project.io/v1alpha1
kind: EnvironmentMapping
metadata:
  # Instance of the EnvrionmentMapping enables
  # Remote Environment in the given namespace.
  # The name must be the same as the name of RemoteEnvironment.
  name: ec-prod
  namespace: production
