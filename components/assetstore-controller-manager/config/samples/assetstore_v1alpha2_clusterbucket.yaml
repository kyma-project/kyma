apiVersion: assetstore.kyma-project.io/v1alpha2
kind: ClusterBucket
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: clusterbucket-sample
spec:
  region: "us-east-1"
  policy: writeonly
