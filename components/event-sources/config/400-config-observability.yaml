apiVersion: v1
kind: ConfigMap
metadata:
  name: config-observability
  namespace: kyma-system
data:
  # See https://github.com/knative/serving/blob/master/config/config-observability.yaml for details
  metrics.backend: prometheus
