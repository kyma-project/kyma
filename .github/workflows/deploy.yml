name: 📰 Deploy VitePress site to GitHub Pages

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'  # Runs at 00:00 UTC every day

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  copy-docs:
    strategy:
      fail-fast: false
      matrix:
        repository:
          - btp-manager
          - istio
          - serverless-manager
          - telemetry-manager
          - eventing-manager
          - api-gateway
          - nats-manager
          - application-connector-manager
          - keda-manager
          - cloud-manager
          - docker-registry
          - busola
          - cli
    runs-on: ubuntu-latest
    steps:
      - name: 🗂️ Checkout ${{matrix.repository}}
        uses: actions/checkout@v4
        with:
          repository: kyma-project/${{matrix.repository}}
          path: ${{matrix.repository}}

      - name: 🚢 Copy for main repo
        run: |
          SOURCE_PATH="${{matrix.repository}}/docs/user"
          TARGET_PATH="./docs/externalContent/${{matrix.repository}}/docs/user"
          SOURCE_PATH_ASSETS="${{matrix.repository}}/docs/assets"
          TARGET_PATH_ASSETS="./docs/externalContent/${{matrix.repository}}/docs/assets"
          if [ -d "$SOURCE_PATH" ]; then
            echo "📁 found in ${{matrix.repository}}, copy to $TARGET_PATH"
            mkdir -p "$TARGET_PATH"
            cp -rf "$SOURCE_PATH/" "$TARGET_PATH/"
              if [ -d "$SOURCE_PATH_ASSETS" ]; then
                echo "↳📁 found in ${{matrix.repository}}, copy to $TARGET_PATH_ASSETS"
                mkdir -p "$TARGET_PATH_ASSETS"
                cp -rf "$SOURCE_PATH_ASSETS/" "$TARGET_PATH_ASSETS/"
              else
                echo "↳🚫 no folder  docs/assets in ${{matrix.repository}}"
              fi
          else
            echo "🚫 No folder docs/users in ${{matrix.repository}}"
          fi
      
      - name: 📦 Upload copied docs
        uses: actions/upload-artifact@v4
        with:
          name: external-docs-${{matrix.repository}}
          path: ./docs/externalContent/${{matrix.repository}}

  build-and-deploy:
    name: 🛠️ Build and Deploy VitePress
    runs-on: ubuntu-latest
    needs: copy-docs
    steps:
      - name: 🗂️ Checkout repository
        uses: actions/checkout@v4
        
      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-btp-manager
          path: docs/externalContent/btp-manager

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-istio
          path: docs/externalContent/istio

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-serverless-manager
          path: docs/externalContent/serverless-manager

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-telemetry-manager
          path: docs/externalContent/telemetry-manager

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-eventing-manager
          path: docs/externalContent/eventing-manager

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-api-gateway
          path: docs/externalContent/api-gateway

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-nats-manager
          path: docs/externalContent/nats-manager

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-application-connector-manager
          path: docs/externalContent/application-connector-manager

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-keda-manager
          path: docs/externalContent/keda-manager

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-cloud-manager
          path: docs/externalContent/cloud-manager

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-docker-registry
          path: docs/externalContent/docker-registry

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-busola
          path: docs/externalContent/busola

      - name: 📥 Download copied docs
        uses: actions/download-artifact@v4
        with:
          name: external-docs-cli
          path: docs/externalContent/cli

      - name: 🪢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: 🛠️ Build VitePress site
        run: npm run docs:build

      - name: ⚙️ Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: 🚀 Upload build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .vitepress/dist

      - name: 🔝 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
