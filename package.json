{
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.12"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "sh ./move_to_public.sh && vitepress build && rm -rf ./docs/public",
    "docs:preview": "vitepress preview"
  },
  "dependencies": {
  }
}
