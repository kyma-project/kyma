apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: cluster-users
  namespace: kyma-system
spec:
  chart:     
    git: https://github.com/piotrmsc/kyma.git
    path: resources/cluster-users
    ref: helm-operator
  releaseName: cluster-users
  valuesFrom:
  - configMapKeyRef:
      name: net-global-overrides-transformed
      namespace: kyma-installer
      key: overrides.yaml
  values:
    global:
      isLocalEnv: false  