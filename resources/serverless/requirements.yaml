dependencies:
  - name: docker-registry
    condition: dockerRegistry.enableInternal
  - name: webhook
    condition: webhook.enabled  
  - name: k3s-tests
    condition: false # this chart is installed manually