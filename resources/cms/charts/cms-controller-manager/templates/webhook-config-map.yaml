apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-config-map
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
data:
  "markdown": |-
    {
      "validations":[],
      "mutations":[]
    }
  "openapi": |-
    {
      "validations":[],
      "mutations":[]
    }
