# Default values for natss-knative-provisioner.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
controller:
  image:
    pullPolicy: IfNotPresent
    location: eu.gcr.io/kyma-project/event-bus/knative/natss/controller:604de5d6
dispatcher:
  image:
    pullPolicy: IfNotPresent
    location: eu.gcr.io/kyma-project/event-bus/knative/natss/dispatcher:604de5d6
    