# Default values for natss-knative-provisioner.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
controller:
  image:
    pullPolicy: IfNotPresent
    location: gcr.io/knative-releases/github.com/knative/eventing/contrib/natss/pkg/controller:v0.4.0
dispatcher:
  image:
    pullPolicy: IfNotPresent
    location: gcr.io/knative-releases/github.com/knative/eventing/contrib/natss/pkg/dispatcher:v0.4.0

