apiVersion: batch/v1
kind: Job
metadata:
  name: istio-kyma-patch
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  backoffLimit: 1
  template:
    metadata:
      name: istio-kyma-patch
    spec:
      serviceAccountName: istio-kyma-patch
      restartPolicy: OnFailure
      containers:
      - name: istio-kyma-patch
        image: kubagoryl/istio-kyma-patch:mtls2
        volumeMounts:
        - name: config-volume
          mountPath: /etc/config
        env:
        - name: CONFIG_DIR
          value: /etc/config
        - name: REQUIRED_ISTIO_VERSION
          value: 1.1.0
      volumes:
      - name: config-volume
        configMap:
          name: istio-kyma-patch-config
