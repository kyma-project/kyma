loki:
  resources:
    limits:
      memory: 512Mi
  config:
    server:
      log_level: warn

fluent-bit:
  resources:
    limits:
      memory: 256Mi

logui:
  resources: 
    requests:
      cpu: 1m
    limits:
      cpu: 30m
