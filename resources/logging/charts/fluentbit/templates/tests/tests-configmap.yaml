{{- if and (.Values.enabled) (.Values.tests.enabled) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fluent-bit.fullname" . }}-tests
  labels:
    {{- include "fluent-bit.metaLabels" . | trim | nindent 4 }}
data:
  run.sh: |-
    @test "fluent-bit tests are not implemented yet" {
      skip "Tests not implemented yet, skipping"
    }
{{- end }}
