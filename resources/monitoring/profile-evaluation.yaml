prometheus:
  prometheusSpec:
    resources:
      limits:
        cpu: 600m
        memory: 2000Mi
      requests:
        cpu: 75m
        memory: 1000Mi
    retention: 6h
    retentionSize: 1GB

grafana:
  persistence:
    enabled: "false"
