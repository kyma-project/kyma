{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Schema definition for velero helm chart values",
    "type": "object",
    "properties": {
        "global": {
            "description": "Configuration of the global properties for velero component",
            "type": "object",
            "properties": {
                "volumeSnapshotLocation": {
                    "description": "Defining the properties for the volume Snapshot Location.",
                    "type": "object",
                    "properties": {
                        "name": {
                            "description": "Specifies the name of the cloud provider used to store volume snapshots, such as aws, gcp, or azure.",
                            "type": "string"
                        },
                        "bucket": {
                            "description": "Specifies the name of the storage bucket where volume snapshots are uploaded.",
                            "type": "string"
                        },
                        "config": {
                            "description": "Configuration of the volumeSnapshotLocation properties",
                            "type": "object",
                            "properties": {
                                "region": {
                                    "description": "Provides the region in which the bucket is created. It only applies to AWS. See the full list of AWS regions.",
                                    "type": "string",
                                    "example": "eu-central-1"
                                },
                                "apiTimeout": {
                                    "description": "Defines the amount of time after which an API request returns a timeout status. It only applies to Azure.",
                                    "type": "string"
                                }
                            }
                        }
                    }
                },
                "backupStorageLocation": {
                    "description": "Defining the properties for the backup Snapshot Location.",
                    "type": "object",
                    "properties": {
                        "name": {
                            "description": "Specifies the name of the cloud provider used to store backups, such as aws, gcp, or azure.",
                            "type": "string"
                        },
                        "bucket": {
                            "description": "Specifies the storage bucket where backups are uploaded.",
                            "type": "string"
                        },
                        "prefix": {
                            "description": "Specifies the directory inside a storage bucket where backups are located.",
                            "type": "string"
                        },
                        "config": {
                            "description": "Configuration of the volumeSnapshotLocation properties",
                            "type": "object",
                            "properties": {
                                "region": {
                                    "description": "Provides the region in which the bucket is created. It only applies to AWS. See the full list of AWS regions.",
                                    "type": "string",
                                    "example": "eu-central-1"
                                },
                                "resourceGroup": {
                                    "description": "Specifies the name of the resource group which contains the storage account for the backup storage location. It only applies to Azure.",
                                    "type": "string"
                                },
                                "storageAccount": {
                                    "description": "Provides the name of the storage account for the backup storage location. It only applies to Azure.",
                                    "type": "string"
                                },
                                "s3ForcePathStyle": {
                                    "description": "Specifies whether to force path style URLs for S3 objects. Set it to true if you use a local storage service like Minio. It only applies to AWS.",
                                    "type": "string"
                                },
                                "s3Url": {
                                    "description": "Specifies the AWS S3 URL. If not provided, Velero generates it from region and bucket. Use this field for local storage services like Minio.",
                                    "type": "string"
                                },
                                "kmsKeyId": {
                                    "description": "Specifies the AWS KMS key ID or alias to enable encryption of the backups stored in S3. It only works with AWS S3 and may require explicitly granting key usage rights.",
                                    "type": "string"
                                },
                                "publicUrl": {
                                    "description": "Specifies the parameter used instead of 3Url when generating download URLs, for example for logs. Use this field for local storage services like Minio.",
                                    "type": "string"
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}