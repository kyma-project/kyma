apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "fullname" . }}
  labels:
    app: {{ template "name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.replicaCount }}
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app: {{ template "name" . }}
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.global.containerRegistry.path }}{{ .Values.image.dir }}/service-catalog-ui:{{ .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.catalog.internalPort }}
            - containerPort: {{ .Values.service.instances.internalPort }}
            - containerPort: {{ .Values.service.brokers.internalPort }}
          volumeMounts:
          - name: config
            mountPath: /var/catalog-public/config
          - name: config
            mountPath: /var/instances-public/config
          - name: config
            mountPath: /var/brokers-public/config
      volumes:
      - name: config
        configMap:
          name: {{ .Chart.Name }}
          items:
          - key: config.js
            path: config.js
