apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gardener.fullname" . }}-test-connection"
  labels:
    {{- include "gardener.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gardener.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
