apiVersion: v1
kind: Service
metadata:
  name: {{ include "gardener.fullname" . }}
  labels:
    {{- include "gardener.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gardener.selectorLabels" . | nindent 4 }}
