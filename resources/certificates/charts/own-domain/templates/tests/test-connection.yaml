apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "own-domain.fullname" . }}-test-connection"
  labels:
    {{- include "own-domain.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "own-domain.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
