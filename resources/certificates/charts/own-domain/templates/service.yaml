apiVersion: v1
kind: Service
metadata:
  name: {{ include "own-domain.fullname" . }}
  labels:
    {{- include "own-domain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "own-domain.selectorLabels" . | nindent 4 }}
