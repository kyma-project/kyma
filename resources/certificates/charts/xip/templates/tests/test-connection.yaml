apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xip.fullname" . }}-test-connection"
  labels:
    {{- include "xip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xip.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
