apiVersion: v1
kind: Service
metadata:
  name: {{ include "xip.fullname" . }}
  labels:
    {{- include "xip.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xip.selectorLabels" . | nindent 4 }}
