
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: dex-cors-route-rule
spec:
  hosts:
  - dex-service
  http:
  - route:
    - destination:
        host: dex-service
    corsPolicy:
      allowOrigin:
        - "*"