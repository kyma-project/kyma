# Default values for asset-metadata-service
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  name: asset-metadata-service
  pullPolicy: IfNotPresent
service:
  internalPort: 3000
  externalPort: 80
  protocol: TCP
metrics:
  scrapeInterval:
virtualservice:
  enabled: false
  annotations: {}
  name: asset-metadata-service
istio:
  namespace: istio-system
