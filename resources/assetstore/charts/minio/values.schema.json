{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Schema definition for minio helm subchart values",
    "type": "object",
    "properties": {
        "secretkey": {
            "description": "Defines the secret key. Add the parameter to set your own secretkey credentials.",
            "example": "By default, secretkey is automatically generated.",
            "type": "string"
        },
        "accesskey": {
            "description": "Defines the access key. Add the parameter to set your own accesskey credentials.",
            "example": "By default, accesskey is automatically generated.",
            "type": "string"
        }
    }
}
