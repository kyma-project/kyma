controller-manager:
  pod:
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
  envs:
    loglevel: "warn"

upload-service:
  pod:
    resources:
      requests:
        cpu: 10m
        memory: 64Mi

asyncapi-service:
  pod:
    resources:
      requests:
        cpu: 10m
        memory: 64Mi

front-matter-service:
  pod:
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
