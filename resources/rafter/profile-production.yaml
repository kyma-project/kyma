controller-manager:
  pod:
    resources:
      requests:
        cpu: 150m
        memory: 64Mi
  envs:
    loglevel: "error"

upload-service:
  pod:
    resources:
      requests:
        cpu: 100m
        memory: 64Mi

asyncapi-service:
  pod:
    resources:
      requests:
        cpu: 100m
        memory: 64Mi

front-matter-service:
  pod:
    resources:
      requests:
        cpu: 100m
        memory: 64Mi