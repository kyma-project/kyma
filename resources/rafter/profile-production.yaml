controller-manager:
  pod:
    resources:
      requests:
        cpu: 200m
        memory: 128Mi

upload-service:
  pod:
    resources:
      requests:
        cpu: 100m
        memory: 64Mi

asyncapi-service:
  pod:
    resources:
      requests:
        cpu: 100m
        memory: 64Mi

front-matter-service:
  pod:
    resources:
      requests:
        cpu: 100m
        memory: 128Mi