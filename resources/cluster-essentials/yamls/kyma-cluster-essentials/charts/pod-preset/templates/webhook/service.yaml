---
# Source: kyma-cluster-essentials/charts/pod-preset/templates/webhook/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: release-name-pod-preset-webhook
  namespace: "default"
  labels:
    app: pod-preset-webhook
    chart: pod-preset-0.1.0
    release: release-name
    heritage: Tiller
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 8443
  selector:
    app: pod-preset-webhook
    release: release-name
