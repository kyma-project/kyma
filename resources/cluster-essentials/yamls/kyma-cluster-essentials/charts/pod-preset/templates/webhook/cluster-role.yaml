---
# Source: kyma-cluster-essentials/charts/pod-preset/templates/webhook/cluster-role.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: release-name-pod-preset-webhook
  labels:
    app: pod-preset-webhook
    chart: pod-preset-0.1.0
    release: release-name
    heritage: Tiller
rules:
- apiGroups:
  - settings.svcat.k8s.io
  resources:
  - podpresets
  verbs:
  - get
  - list
  - watch
