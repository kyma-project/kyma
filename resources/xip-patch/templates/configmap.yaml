apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ .Release.Namespace }}
  name: kyma-cert-manager-config
data:
  config.yaml.tpl: |-
{{.Files.Get "files/cert-manager-config.yaml.tpl" | printf "%s" | indent 4}}

