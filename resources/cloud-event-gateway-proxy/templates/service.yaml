apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-event-gateway-proxy.fullname" . }}
  labels:
    {{- include "cloud-event-gateway-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-event-gateway-proxy.selectorLabels" . | nindent 4 }}
