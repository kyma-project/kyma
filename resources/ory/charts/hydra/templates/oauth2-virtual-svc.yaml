apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ template "fullname" . }}-oauth2
  namespace: {{ .Release.Namespace }}
spec:
  gateways:
    - {{ .Values.global.gateway }}
  hosts:
    - {{ .Values.hydraServer.host.oauth2.name }}.kyma.local
  http:
    - match:
        - uri:
            regex: /.*
      route:
        - destination:
            host: oauth2
            port:
              number: {{ .Values.hydraServer.service.port }}

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ template "fullname" . }}-oauth2-admin
  namespace: {{ .Release.Namespace }}
spec:
  gateways:
    - {{ .Values.global.gateway }}
  hosts:
    - {{ .Values.hydraServer.host.admin.name }}.kyma.local
  http:
    - match:
        - uri:
            regex: /.*
      route:
        - destination:
            host: oauth2
            port:
              number: {{ .Values.hydraServer.service.adminPort }}
