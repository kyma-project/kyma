loginConsent:
  port: 3000
  image:
    repository: oryd/hydra-login-consent-node
    tag: v1.0.0-rc.6
    pullPolicy: IfNotPresent
  config:
    hydraAdminUrl: http://oauth2.kyma-system.svc.cluster.local:4445
  service:
    type: ClusterIP
    port: 3000
  host:
    name: oauth2-login-consent.kyma.local

hydraServer:
  port: 4444
  adminPort: 4445
  image:
    repository: oryd/hydra
    tag: v1.0.0-rc.8
    pullPolicy: IfNotPresent
  config:
    oauth2:
      shareErrorDebug: "1"
    databaseUrl: memory
    systemSecret: "youReallyNeedToChangeThis"
    oidc:
      subjectTypesSupported: "public,pairwise"
      subjectTypePairwiseSalt: "youReallyNeedToChangeThis"
  service:
    type: ClusterIP
    port: 80
    adminPort: 4445
  host:
    oauth2:
      name: oauth2.kyma.local
    admin:
      name: oauth2-admin.kyma.local