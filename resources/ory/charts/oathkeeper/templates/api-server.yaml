apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "fullname" . }}-api-server
  namespace: {{ .Release.Namespace }}
  labels:
    kyma-project.io/installation: ""
spec:
  template:
    metadata:
      labels:
        name: {{ template "fullname" . }}
        app: {{ template "fullname" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}-api-server
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - "oathkeeper"
          args:
            - "serve"
            - "api"
          env:
            - name: CREDENTIALS_ISSUER_ID_TOKEN_HS256_SECRET
              value: {{ .Values.credentialsIssuerID.token }}
            - name: PORT
              value: {{ quote .Values.api-server.port }}
            - name: DATABASE_URL
              value: {{ .Values.api-server.config.databaseUrl }}
            - name: AUTHENTICATOR_OAUTH2_INTROSPECTION_URL
              value: {{ .Values.introspectionServer.url }}
          ports:
            - containerPort: {{ .Values.api-server.port }}
