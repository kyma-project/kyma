apiVersion: v1
kind: Service
metadata:
  name: {{ include "oathkeeper.fullname" . }}
  labels:
    app: {{ include "oathkeeper.name" . }}
    chart: {{ include "oathkeeper.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "oathkeeper.name" . }}
    release: {{ .Release.Name }}
