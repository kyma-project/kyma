apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "login-consent.fullname" . }}-test-connection"
  labels:
    {{- include "login-consent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "login-consent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
