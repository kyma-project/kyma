apiVersion: v1
kind: ConfigMap
metadata:
  name: app-broker-config-map-mocked
  labels:
    app: {{ .Chart.Name }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
{{ if .Values.config }}
data:
  config.yaml: |-
{{ .Values.config | toYaml | indent 4 }}
{{ end }}
