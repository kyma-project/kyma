apiVersion: applicationconnector.kyma-project.io/v1alpha1
kind: KymaGroup
metadata:
  name: default
spec:
  tenant: "default"
  cluster:
    appRegistryUrl: "https://gateway.{{ .Values.global.domainName }}/%s/metadata/services"
    eventsUrl: "https://gateway.{{ .Values.global.domainName }}/%s/events"
  applications: []