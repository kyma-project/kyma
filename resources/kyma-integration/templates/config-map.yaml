---
apiVersion: v1
kind: ConfigMap
metadata:
  name: test-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: updatefun
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
data:
  httpEndpoint: 0.0.0.0:{{ .Values.containerPort }}
  issuer: https://testing.{{ .Values.global.domainName }}
  limit: {{ .Values.fun.limit }}
  logger-level: debug
  logger-format: text

