kind: Service
apiVersion: v1
metadata:
  name: bindings-webhhook-pod
spec:
  selector:
    app: binding-controller
  ports:
    - protocol: TCP
      port: 443
      targetPort: {{ .Values.config.bindingController.port }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: binding-controller
  labels:
    app: binding-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: binding-controller
  template:
    metadata:
      labels:
        app: binding-controller
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      serviceAccountName: binding
      containers:
        - name: manager
          image: "{{ .Values.images.containerRegistry.path }}/binding:{{ .Values.images.bindingController.version }}"
          ports:
            - containerPort: {{ .Values.config.bindingController.port }}
          imagePullPolicy: Always
          volumeMounts:
          - name: bindings-webhook-cert
            mountPath: /var/run/binding-pod-webhook
            readOnly: true
          env:
          - name: APP_PORT
            value: "{{ .Values.config.bindingController.port }}"
      volumes:
      - name: bindings-webhook-cert
        secret:
          secretName: bindings-webhook-cert
          items:
          - key: tls.crt
            path: tls.crt
          - key: tls.key
            path: tls.key