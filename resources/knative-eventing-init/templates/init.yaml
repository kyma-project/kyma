apiVersion: v1
data:
  default-ch-config: |
    clusterDefault:
      apiVersion: {{ .Values.eventing.defaultChannel.apiVersion }}
      kind: {{ .Values.eventing.defaultChannel.kind }}
kind: ConfigMap
metadata:
  name: default-ch-webhook
  namespace: knative-eventing
