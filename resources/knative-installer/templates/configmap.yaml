apiVersion: v1
kind: ConfigMap
metadata:
  name: knative-installer-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: knative-installer
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
data:
  SERVING_URL: https://github.com/knative/serving/releases/download/v0.2.1/release-no-mon.yaml
  EVENTING_URL: https://github.com/knative/eventing/releases/download/v0.2.0/release.yaml
