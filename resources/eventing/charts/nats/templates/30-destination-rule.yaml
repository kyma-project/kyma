{{- if .Values.install.enabled }}

apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: {{ .Values.nats.fullname }}
spec:
  host: {{ .Values.nats.fullname }}.{{ .Release.Namespace }}.svc.cluster.local
  trafficPolicy:
    tls:
      mode: DISABLE

 {{- end }}
