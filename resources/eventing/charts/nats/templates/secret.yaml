apiVersion: v1
kind: Secret
metadata:
  name: "sys-password"
type: Opaque
stringData:
  accounts.json: |-
    accounts: {
      SYS: {
        users: [
          {user: admin, password: admin}
        ]
      },
    }
    system_account: SYS
