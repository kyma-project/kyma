apiVersion: v1
data:
  client-id: "{{ .Values.oeb.oauthClientId | b64enc }}"
  client-secret: "{{ .Values.oeb.oauthClientSecret | b64enc }}"
  token-endpoint: "{{ .Values.oeb.oauthTokenEndpoint | b64enc }}"
  ems-publish-url: "{{ .Values.oeb.publishUrl | b64enc }}"
kind: Secret
metadata:
  labels:
    app: event-publisher-proxy
  name: event-publisher-proxy
