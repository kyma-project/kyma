{{ if eq .Values.global.domainName .Values.domainName }}
apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: {{ template "name" . }}-tls-cert
data:
  "tls.crt": {{ .Values.global.tlsCrt }}
  "tls.key": {{ .Values.global.tlsKey }}
{{ end  }}