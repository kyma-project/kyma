apiVersion: v1
kind: Service
metadata:
  annotations:
  name: {{ template "name" . }}-ssl
  namespace: {{ .Release.Namespace }}
spec:
  type: LoadBalancer
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 8443
  selector:
    app: {{ template "name" . }}
  
