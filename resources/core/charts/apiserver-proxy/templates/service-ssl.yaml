apiVersion: v1
kind: Service
metadata:
  annotations:
  name: {{ template "name" . }}-ssl
  namespace: {{ .Release.Namespace }}
spec:
  type: LoadBalancer
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: {{ .Values.port.secure }}
  selector:
    app: {{ template "name" . }}
  
