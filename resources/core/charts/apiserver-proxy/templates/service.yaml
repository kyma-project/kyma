apiVersion: v1
kind: Service
metadata:
  annotations:
  name: {{ template "name" . }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: http
    port: {{ .Values.port.insecure }}
    protocol: TCP
    targetPort: {{ .Values.port.insecure }}
  - name: http-metrics
    port: 2112
    protocol: TCP
    targetPort: 2112
  selector:
    app: {{ template "name" . }}
  
