apiVersion: v1
kind: ConfigMap
metadata:
  name: test-kubeless-config
  namespace: kyma-system
  labels:
    helm-chart-test: "true"
  annotations:
    helm.sh/hook: test-success
data:
  k8sYaml: |-
    {{- include "k8s.yaml.tpl" . | indent 4}}
  svcBindLambdaYaml: |-
    {{- include "svcbind-lambda.yaml.tpl" . | indent 4}}
