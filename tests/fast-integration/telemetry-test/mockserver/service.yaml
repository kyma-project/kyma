apiVersion: v1
kind: Service
metadata:
  labels:
    app: mockserver
  name: mockserver
  namespace: mockserver
spec:
  ports:
  - name: serviceport
    port: 1080
    targetPort: serviceport
  selector:
    app: mockserver
  type: ClusterIP
