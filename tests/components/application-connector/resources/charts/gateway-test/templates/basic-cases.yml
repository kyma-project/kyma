apiVersion: applicationconnector.kyma-project.io/v1alpha1
kind: Application
metadata:
  name: basic-cases
  namespace: {{ .Values.namespace }}
spec:
  description: This is the test service
  skipVerify: true
  labels:
    app: basic-cases
  services:
    - displayName: unsecure-always-ok
      name: unsecure-always-ok
      providerDisplayName: AlwaysOK
      description: Should return 200 when call to unsecure is ok
      id: "{{ uuidv4 }}"
      entries:
        - type: API
          targetUrl: "http://{{ .Values.mockServiceName }}.{{ .Values.namespace  }}.svc.cluster.local:8080/v1/api/unsecure/ok"
          centralGatewayUrl: "http://central-application-gateway.kyma-system:8080/basic-cases/unsecure-always-ok"
    - displayName: basic-auth-ok
      name: basic-auth-ok
      providerDisplayName: Basic
      description: Should return 200 when call to basic Auth is ok
      id: "{{ uuidv4 }}"
      entries:
        - type: API
          targetUrl: "http://{{ .Values.mockServiceName }}.{{ .Values.namespace  }}.svc.cluster.local:8080/v1/api/basic/ok"
          centralGatewayUrl: "http://central-application-gateway.kyma-system:8080/basic-cases/basic-auth-ok"
          credentials:
            secretName: basic-test
            type: Basic
---
apiVersion: v1
kind: Secret
metadata:
  name: basic-test
  namespace: kyma-integration
type: Opaque
data:
  password: {{ "passwd" | b64enc }}
  username: {{ "user" | b64enc }}
