apiVersion: applicationconnector.kyma-project.io/v1alpha1
kind: Application
metadata:
  name: validator-test-app
  namespace: "{{ .Values.namespace }}"
spec:
  description: Application for the Application Connectivity Validator testing purposes
  labels:
    app: validator-test-app
  skipVerify: true
  compassMetadata:
    authentication:
      clientIds:
        - "clientID"