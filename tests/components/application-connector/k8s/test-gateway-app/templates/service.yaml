apiVersion: v1
kind: Service
metadata:
  labels:
    app: test-gateway-app
  name: test-app-srv
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - name: my-https-port
      port: 8080
      targetPort: 8080
  selector:
    app: test-gateway-app
  type: ClusterIP