apiVersion: applicationconnector.kyma-project.io/v1alpha1
kind: Application
metadata:
  name: test-app
  namespace: "{{ .Values.namespace }}"
spec:
  description: This is the test service
  skipVerify: true
  labels:
    app: test-app
  services:
    - displayName: always ok
      name: alwaysok
      providerDisplayName: Rafał
      description: Always Ok in testapp
      id: "{{ uuidv4 }}"
      entries:
        - type: API
          targetUrl: "http://test-app.{{ .Values.namespace  }}.svc.cluster.local:8080/v1/api/unsecure/ok"
          centralGatewayUrl: "http://central-application-gateway.kyma-system:8080/test-app/always-ok"
    - displayName: basic
      name: basic
      providerDisplayName: Rafał
      description: Basic Auth Optimistic scenario
      id: "{{ uuidv4 }}"
      entries:
        - type: API
          targetUrl: "http://test-app.{{ .Values.namespace  }}.svc.cluster.local:8080/v1/api/basic/ok"
          centralGatewayUrl: "http://central-application-gateway.kyma-system:8080/test-app/basic"
          credentials:
            secretName: basic-test
            type: Basic
    - displayName: oauth
      name: oauth
      providerDisplayName: Rafał
      description: OAuth Optimistic Scenario
      id: "{{ uuidv4 }}"
      entries:
        - type: API
          targetUrl: "http://test-app.{{ .Values.namespace  }}.svc.cluster.local:8080/v1/api/oauth/ok"
          centralGatewayUrl: "http://central-application-gateway.kyma-system:8080/test-app/oauth"
          credentials:
            secretName: oauth-test
            authenticationUrl: "http://test-app.{{ .Values.namespace  }}.svc.cluster.local:8080/v1/api/oauth/token"
            type: OAuth
    - displayName: mtls oauth
      name: mtls-oauth
      providerDisplayName: Rafał
      description: mTLS OAuth Optimistic Scenario
      id: "{{ uuidv4 }}"
      entries:
        - type: API
          targetUrl: "https://test-app.{{ .Values.namespace  }}.svc.cluster.local:8090/v1/api/mtlsoauth/ok"
          centralGatewayUrl: "http://central-application-gateway.kyma-system:8080/test-app/mtls-oauth"
          credentials:
            secretName: mtls-oauth-test
            authenticationUrl: "https://test-app.{{ .Values.namespace  }}.svc.cluster.local:8090/v1/api/mtlsoauth/token"
            type: OAuthWithCert
    - displayName: csrf basic
      name: csrf-basic
      providerDisplayName: Rafał
      description: Basic Auth with CSRF Optimistic Scenario
      id: "{{ uuidv4 }}"
      entries:
        - type: API
          targetUrl: "http://test-app.{{ .Values.namespace  }}.svc.cluster.local:8080/v1/api/csrf-basic/ok"
          centralGatewayUrl: "http://central-application-gateway.kyma-system:8080/test-app/csrf-basic"
          credentials:
            secretName: basic-test
            type: Basic
            csrfInfo:
              tokenEndpointURL: "http://test-app.{{ .Values.namespace  }}.svc.cluster.local:8080/v1/api/csrf/token"
---
apiVersion: v1
kind: Secret
metadata:
  name: basic-test
  namespace: kyma-integration
type: Opaque
data:
  password: {{ "passwd" | b64enc }}
  username: {{ "user" | b64enc }}
---
apiVersion: v1
kind: Secret
metadata:
  name: oauth-test
  namespace: kyma-integration
data:
  clientId: {{ "clientID" | b64enc }}
  clientSecret: {{ "clientSecret" | b64enc }}
---
apiVersion: v1
kind: Secret
metadata:
  name: mtls-oauth-test
  namespace: kyma-integration
data:
  clientId: {{ "clientID" | b64enc }}
  key: {{ .Files.Get "certs/client.key" | b64enc }}
  crt: {{ .Files.Get "certs/client.crt" | b64enc }}
