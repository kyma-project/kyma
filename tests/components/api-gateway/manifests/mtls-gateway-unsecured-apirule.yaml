apiVersion: gateway.kyma-project.io/v1alpha1
kind: APIRule
metadata:
  name: "{{.NamePrefix}}-{{.TestID}}"
  namespace: "{{.Namespace}}"
spec:
  service:
    host: "httpbin-{{.TestID}}.mtls.{{.Domain}}"
    name: httpbin-{{.TestID}}
    port: 8000
  gateway: "{{.GatewayNamespace}}/{{.GatewaymTLSName}}"
  rules:
  - path: /.*
    methods: ["GET"]
    accessStrategies:
    - handler: noop
    mutators: []
